<style>
    /* === Common CSS Variables === */
    :root {
        --bg-color: #121212;
        --primary-text-color: #FFFFFF;
        --secondary-text-color: #a0a0a0;
        --card-bg-color: #1a1a1a;
        --border-color: #333333;
        --accent-color: #4a90e2;
        --deal-color: #e24a4a;
        --main-font: 'Manrope', 'Vazirmatn', sans-serif;
        --text-color: var(--primary-text-color); /* اضافه شده برای سازگاری با متن محتوا */
    }
    body.light-mode {
        --bg-color: #f4f4f5;
        --card-bg-color: #FFFFFF;
        --primary-text-color: #18181b;
        --secondary-text-color: #71717a;
        --border-color: #e4e4e7;
        --text-color: var(--primary-text-color); /* اضافه شده برای سازگاری با متن محتوا */
    }
    /* === Base Styles === */
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }
    html {
        scroll-behavior: smooth;
    }
    body {
        font-family: var(--main-font);
        background-color: var(--bg-color);
        color: var(--primary-text-color);
        line-height: 1.7;
        overflow-x: hidden;
        transition: background-color 0.3s ease, color 0.3s ease;
    }
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
    }
    a {
        text-decoration: none;
        color: var(--primary-text-color);
        transition: color 0.3s ease;
    }
    a:hover {
        color: var(--accent-color);
    }
    img {
        max-width: 100%;
        height: auto;
        display: block;
    }
    /* === Header Styles === */
    .promo-banner {
        background-color: var(--accent-color);
        color: var(--primary-text-color);
        text-align: center;
        padding: 8px 0;
        font-weight: 700;
        font-size: 0.9rem;
    }
    .main-header {
        background-color: rgba(18, 18, 18, 0.8);
        border-bottom: 1px solid var(--border-color);
        padding: 15px 0;
        position: sticky;
        top: 0;
        width: 100%;
        z-index: 1000;
        backdrop-filter: blur(10px);
        transition: background-color 0.3s ease, border-color 0.3s ease;
    }
    body.light-mode .main-header {
        background-color: rgba(229, 231, 235, 0.8);
    }
    .header-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .logo {
        font-size: 2rem;
        font-weight: 800;
        letter-spacing: -1px;
    }
    .main-nav ul {
        list-style: none;
        display: flex;
    }
    .main-nav ul li {
        margin-right: 30px;
    }
    .main-nav a {
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 8px;
    }
   
    .main-nav a i {
        font-size: 1rem;
        color: var(--accent-color);
        transition: transform 0.3s ease;
    }
   
    .main-nav a:hover i {
        transform: scale(1.1);
    }
    .header-actions {
        display: flex;
        align-items: center;
        gap: 25px;
        font-size: 1.3rem;
    }
    .header-action-link {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.9rem;
        font-weight: 700;
    }
    .header-action-link span {
        display: inline;
    }
    #theme-toggle {
        cursor: pointer;
    }
    #theme-toggle .fa-sun {
        display: none;
    }
    body.light-mode #theme-toggle .fa-sun {
        display: inline-block;
    }
    body.light-mode #theme-toggle .fa-moon {
        display: none;
    }
    /* === Footer Styles === */
    .main-footer {
        background-color: #000000;
        color: var(--secondary-text-color);
        padding: 80px 0 30px;
        border-top: 1px solid var(--border-color);
        transition: background-color 0.3s ease, border-color 0.3s ease;
    }
    body.light-mode .main-footer {
        background-color: #18181b;
    }
    .footer-content {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 40px;
    }
    .footer-section h4 {
        color: var(--primary-text-color);
        margin-bottom: 20px;
        font-size: 1.2rem;
    }
    .footer-section ul {
        list-style: none;
    }
    .footer-section li {
        margin-bottom: 10px;
    }
    .newsletter-form {
        display: flex;
        margin-top: 20px;
    }
    .newsletter-form input {
        flex-grow: 1;
        background: #333;
        border: 1px solid var(--border-color);
        color: var(--primary-text-color);
        padding: 10px;
        border-radius: 0;
    }
    body.light-mode .newsletter-form input {
        background-color: #f4f4f5;
        border-color: #d4d4d8;
    }
    .newsletter-form button {
        background: var(--accent-color);
        border: 1px solid var(--accent-color);
        color: white;
        padding: 10px 15px;
        cursor: pointer;
        font-weight: 700;
    }
    .footer-bottom {
        text-align: center;
        margin-top: 60px;
        padding-top: 30px;
        border-top: 1px solid var(--border-color);
        font-size: 0.9rem;
    }
    .trust-badges {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 20px;
        margin-bottom: 20px;
    }
    .trust-badges img {
        height: 40px;
        filter: grayscale(100%);
        opacity: 0.7;
        transition: all 0.3s ease;
    }
    .trust-badges img:hover {
        filter: grayscale(0%);
        opacity: 1;
    }
    .instagram-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 5px;
    }
    .instagram-grid img {
        width: 80px;
        height: 80px;
        object-fit: cover;
    }
    /* === Common Page Styles === */
    .page-header {
        padding: 60px 0;
        background-color: var(--card-bg-color);
        text-align: center;
        border-bottom: 1px solid var(--border-color);
        transition: background-color 0.3s ease, border-color 0.3s ease;
    }
    .page-header h1 {
        font-size: 3rem;
        font-weight: 800;
    }
    .breadcrumb {
        margin-top: 10px;
        color: var(--secondary-text-color);
    }
    .breadcrumb a {
        color: var(--secondary-text-color);
    }
    /* === Common Button Styles === */
    .btn {
        display: inline-block;
        text-align: center;
        background-color: var(--accent-color);
        color: white;
        border: 2px solid var(--accent-color);
        padding: 12px 30px;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 0.9rem;
    }
    .btn:hover {
        background-color: transparent;
        color: var(--accent-color);
    }
    .btn-secondary {
        background: none;
        border-color: var(--secondary-text-color);
        color: var(--secondary-text-color);
    }
    .btn-secondary:hover {
        border-color: var(--accent-color);
        color: var(--accent-color);
    }
    .btn-outline {
        background: none;
        color: var(--accent-color);
    }
    /* === Mobile Menu === */
    .mobile-menu {
        display: none;
        position: fixed;
        top: 0;
        right: -100%;
        width: 280px;
        height: 100vh;
        background: var(--card-bg-color);
        border-left: 1px solid var(--border-color);
        padding: 20px;
        transition: right 0.3s ease;
        z-index: 2000;
        overflow-y: auto;
    }
   
    .mobile-menu.active {
        right: 0;
    }
   
    .mobile-menu-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: rgba(0, 0, 0, 0.5);
        z-index: 1999;
    }
   
    .mobile-menu-overlay.active {
        display: block;
    }
   
    .mobile-menu-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
        padding-bottom: 15px;
        border-bottom: 1px solid var(--border-color);
    }
   
    .mobile-menu-close {
        font-size: 1.5rem;
        cursor: pointer;
    }
   
    .mobile-menu ul {
        list-style: none;
    }
   
    .mobile-menu ul li {
        margin-bottom: 15px;
    }
   
    .mobile-menu ul li a {
        display: flex;
        align-items: center;
        gap: 15px;
        padding: 12px;
        font-weight: 700;
        border-radius: 8px;
        transition: all 0.3s ease;
    }
   
    .mobile-menu ul li a i {
        font-size: 1.2rem;
        width: 24px;
        text-align: center;
        color: var(--accent-color);
    }
   
    .mobile-menu ul li a:hover {
        background: rgba(74, 144, 226, 0.1);
        color: var(--accent-color);
    }
   
    .mobile-menu ul li a:hover i {
        transform: scale(1.1);
    }
   
    .mobile-menu-toggle {
        display: none;
        cursor: pointer;
    }
    /* === Responsive Design === */
    @@media (max-width: 992px) {
        .main-nav, .header-action-link span {
            display: none;
        }
       
        .mobile-menu-toggle {
            display: block;
        }
       
        .mobile-menu {
            display: block;
        }
    }
    @@media (max-width: 768px) {
        .header-actions {
            gap: 20px;
        }
       
        .page-header h1 {
            font-size: 2.5rem;
        }
    }
    /* === Theme Transition === */
    section, .card-bg-color, .main-header, .feature-card, .product-card, .look-card, .testimonial-card, .blog-card, .time-block, .main-footer {
        transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
    }
</style>