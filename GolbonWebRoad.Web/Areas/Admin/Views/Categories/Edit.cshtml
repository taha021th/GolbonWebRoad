@model GolbonWebRoad.Web.Areas.Admin.Models.Categories.EditCategoryViewModel
@{
    ViewData["Title"] = "ویرایش دسته‌بندی: " + Model.Name;
}
<html>
<head>
    <meta name="robots" content="noindex, nofollow" />
</head>
<body>
<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card shadow-sm">
            <div class="card-header">
                <h3 class="mb-0">@ViewData["Title"]</h3>
            </div>
            <div class="card-body">
                <form asp-action="Edit" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    

                    <div class="mb-3">
                        <label asp-for="Slog" class="form-label"></label>
                        <input asp-for="Slog" class="form-control" />
                        <span asp-validation-for="Slog" class="text-danger"></span>
                    </div>

                    <!-- فیلد جدید: Content -->
                    <div class="mb-3">
                        <label asp-for="Content" class="form-label">محتوا</label>
                        <textarea asp-for="Content" class="form-control" rows="6"></textarea>
                        <span asp-validation-for="Content" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="NewImage" class="form-label">تصویر جدید (اختیاری)</label>
                        <input asp-for="NewImage" type="file" class="form-control" accept="image/*" />
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ExistingImage))
                    {
                        <div class="mb-3">
                            <label class="form-label d-block">تصویر فعلی</label>
                            <img src="@Model.ExistingImage" alt="تصویر فعلی" class="img-thumbnail" style="max-width: 150px;" />
                        </div>
                    }

                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a asp-action="Index" class="btn btn-secondary">بازگشت به لیست</a>
                        <button type="submit" class="btn btn-primary">ذخیره تغییرات</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
</body>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
</html>