@model IEnumerable<GolbonWebRoad.Web.Areas.Admin.Models.Brands.BrandViewModel>
@{
    ViewData["Title"] = "مدیریت برندها";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0 text-gray-800">@ViewData["Title"]</h1>
</div>

<div class="row g-4">
    <div class="col-lg-5">
        <div class="card shadow-sm">
            <div class="card-header">
                افزودن برند جدید
            </div>
            <div class="card-body">
                @* The form posts to the Create action of BrandsController *@
                <form asp-area="Admin" asp-controller="Brands" asp-action="Create" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()

                    <div class="mb-3">
                        <label for="Name" class="form-label">نام برند</label>
                        <input name="Name" id="Name" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label for="Content" class="form-label">محتوا</label>
                        <input name="Content" id="Content" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label for="Slog" class="form-label">اسلاگ</label>
                        <input name="Slog" id="Slog" class="form-control" required />
                    </div>

                    <div class="mb-3">
                        <label for="Image" class="form-label">تصویر برند (اختیاری)</label>
                        <input type="file" name="Image" id="Image" class="form-control" accept="image/*" />
                    </div>

                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">ذخیره برند</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-7">
        <div class="card shadow-sm">
            <div class="card-header">
                لیست برندها
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th class="py-3 px-4">تصویر</th>
                                <th class="py-3 px-4">نام برند</th>
                                <th class="py-3 px-4 text-center">عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="align-middle px-4" style="width:100px;">
                                            @if (!string.IsNullOrWhiteSpace(item.ImageUrl))
                                            {
                                                <img src="@item.ImageUrl" alt="@item.Name" class="img-thumbnail" style="max-height:50px; max-width:90px; object-fit:cover;" />
                                            }
                                            else
                                            {
                                                <img src="https://placehold.co/90x50?text=No+Image" class="img-thumbnail" alt="No image" />
                                            }
                                        </td>
                                        <td class="align-middle px-4">@item.Name</td>
                                        <td class="align-middle text-center px-4">
                                            <a asp-area="Admin" asp-controller="Brands" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary" title="ویرایش">
                                                <i class="fas fa-pen"></i>
                                            </a>
                                            <a asp-area="Admin" asp-controller="Brands" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger" title="حذف">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="3" class="text-center py-4 text-muted">هیچ برندی یافت نشد.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>